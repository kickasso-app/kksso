import PreviewClient from './PreviewClient';

export const metadata = {
  title: 'Studio Preview - Arti',
  description: 'Preview how your studio appears to visitors.',
};

export default function PreviewPage() {
  return <PreviewClient />;
}
