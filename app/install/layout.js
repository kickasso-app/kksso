export const metadata = {
  title: "Install App | Arti",
  description: "Install the Arti app on your device for the best experience.",
};

export default function InstallLayout({ children }) {
  return <>{children}</>;
}
